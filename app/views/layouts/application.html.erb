<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/4e9fd11c74.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <header>
      <% if flash[:errors] %>
        <%= flash[:errors].count %>件のエラーが発生しました
        <ul>
          <% flash[:errors].each do |key, value| %>
            <li><%= value[0] %></li>
          <% end %>
        </ul>
      <% end %>
      <li><%= link_to "Bookers", root_path() %></li>
      <% if user_signed_in? %>
        <li>
          <span class="fa-solid fa-home"></span><%= link_to "Home", user_path(current_user.id) %>
        </li>
        <li>
          <span class="fa-solid fa-users"></span><%= link_to "Users", users_path() %>
        </li>
        <li>
          <span class="fa-solid fa-book-open"></span><%= link_to "Books", books_path() %>
        </li>
        <li>
          <span class="fa-solid fa-sign-out-alt"></span><%= link_to "Log out", destroy_user_session_path, method: :delete %>
        </li>
      <% else %>
        <li>
          <span class="fa-solid fa-home"><%= link_to "Home", root_path() %>
        </li>
        <li>
          <span class="fa-solid fa-link"></span><%= link_to "About", about_path() %>
        </li>
        <li>
          <span class="fa-solid fa-user-plus"></span><%= link_to "Sign up", new_user_registration_path %>
        </li>
        <li>
          <span class="fa-solid fa-sign-in-alt"></span><%= link_to "Log in", new_user_session_path %>
        </li>
      <% end %>
    </header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
